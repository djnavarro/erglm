% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-helpers.R
\name{erglm_link}
\alias{erglm_link}
\alias{erglm_invlink}
\title{Link and inverse-link functions for a fitted model}
\usage{
erglm_link(mod)

erglm_invlink(mod)
}
\arguments{
\item{mod}{A fitted model, typically an \code{erglm_model}/\code{glm} object.}
}
\value{
A function of one numeric-vector argument.
}
\description{
Every \code{glm()} family already carries its link and inverse-link
functions (\code{stats::family(mod)$linkfun} / \code{stats::family(mod)$linkinv}),
but many users don't realise these are available for the taking.
\code{erglm_link()} and \code{erglm_invlink()} are thin, discoverable wrappers
around them: \code{erglm_link()} maps the response scale to the linear
predictor scale, and \code{erglm_invlink()} maps the linear predictor
scale back to the response scale.
}
\examples{
mod <- erglm_model(ae1 ~ aucss + sex, erglm_data, family = binomial())
erglm_link(mod)(0.5)
erglm_invlink(mod)(0)
erglm_link(mod)(erglm_invlink(mod)(-2:2))
}
