% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/erglm-core.R
\name{erglm_predict}
\alias{erglm_predict}
\title{Predictions and confidence intervals for exposure-response models}
\usage{
erglm_predict(object, newdata = NULL, conf_level = 0.95)
}
\arguments{
\item{object}{An erglm model, as returned by \code{\link[=erglm_model]{erglm_model()}}}

\item{newdata}{Data frame containing cases to be predicted}

\item{conf_level}{Confidence level for the intervals}
}
\value{
A tibble
}
\description{
Predictions and confidence intervals for exposure-response models
}
\details{
Computes intervals on the link scale and back-transforms with
\code{stats::family(object)$linkinv}, so this works for any \code{glm()} family,
not just binomial/logistic models. See also \code{\link[=erglm_fun]{erglm_fun()}} for
generating predictions at arbitrary (possibly counterfactual)
parameters or data.

This is a tidy, opinionated alternative to calling base R's
\code{predict()} directly on \code{object} -- since \code{object} is a genuine
\code{glm} object, \code{predict()} (and \code{predict(object, se.fit = TRUE)}, on
which this function is based) work unchanged and remain useful for
quick point estimates or when a tidy data frame isn't needed. See
\code{vignette("methods", package = "erglm")} for a side-by-side
comparison and other inherited \code{glm}/\code{lm} methods (\code{summary()},
\code{vcov()}, \code{AIC()}, etc.).
}
\examples{
mod <- erglm_model(ae1 ~ aucss, erglm_data, family = binomial())
prd <- erglm_predict(mod, erglm_data)
prd

mod_gauss <- erglm_model(biomarker_change ~ aucss, erglm_data, family = gaussian())
erglm_predict(mod_gauss, erglm_data)

}
