% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/erglm-core.R
\name{erglm_model}
\alias{erglm_model}
\title{Fit an exposure-response model based on \code{glm()}}
\usage{
erglm_model(formula, data, family = stats::gaussian(), ...)
}
\arguments{
\item{formula}{Model formula}

\item{data}{Data set}

\item{family}{The error distribution and link function to use, as for
\code{stats::glm()}. Defaults to \code{stats::gaussian()}, matching \code{stats::glm()}'s
own default. Tested and officially supported for \code{binomial()},
\code{poisson()}, \code{gaussian()}, and \code{Gamma()}; other \code{glm()} families should
work through the same generic mechanisms but are untested.}

\item{...}{Other arguments passed to \code{glm()}. Note that \code{weights},
\code{subset}, and \code{offset} don't work reliably here -- see Details below.}
}
\value{
A glm object
}
\description{
Fit an exposure-response model based on \code{glm()}
}
\details{
The returned object has class \code{c("erglm_model", "glm", "lm")}:
it \emph{is} a \code{glm} object, with a little extra metadata attached. This
means all of the usual \code{glm}/\code{lm} methods work unchanged, without
needing an erglm-specific equivalent -- e.g. \code{summary()}, \code{coef()},
\code{vcov()}, \code{confint()}, \code{predict()}, \code{AIC()}, \code{BIC()}, \code{logLik()}, and
\code{anova()} for comparing nested models. See \code{vignette("methods", package = "erglm")} for worked examples of these. \code{erglm_predict()}
is a separate, erglm-specific alternative to \code{predict()} that
returns confidence intervals on the response scale in a tidy data
frame; the two are complementary, not competing.

\code{weights}, \code{subset}, and \code{offset} can't currently be passed through
\code{...} to \code{stats::glm()}: \code{glm()} resolves these non-standard-evaluation
arguments via \code{match.call()}, which breaks once they've been forwarded
through another function's \code{...} rather than named directly in the
call \code{glm()} itself sees. This reproduces with a trivial wrapper
(\code{function(formula, data, family, ...) glm(formula, data, family, ...)}) and is a limitation of \code{glm()}/\code{lm()}'s NSE, not something
specific to erglm's \code{family} generalisation -- see e.g. the "Note"
in \code{?lm} about wrapping \code{lm()}. Attempting it currently fails with a
low-level error (\code{"..1 used in an incorrect context, no ... to look in"}). Two workarounds: fold an offset into the formula itself (e.g.
\code{y ~ x + offset(z)}) rather than passing \verb{offset =}, and pre-filter
\code{data} yourself rather than passing \verb{subset =}. There's no similar
formula-level workaround for \code{weights}; call \code{stats::glm(formula, data, family, weights = ...)} directly instead, then (if you want
the \code{erglm_model} class for consistency, e.g. for \code{simulate()}'s S3
dispatch) run \code{class(mod) <- c("erglm_model", class(mod))} on the
result -- every other erglm function works on a plain \code{glm} object
just as well, since none of them require the class specifically.
}
\examples{
mod <- erglm_model(ae1 ~ aucss, erglm_data, family = binomial())
mod

# other glm() families are also supported
mod_pois <- erglm_model(ae_count ~ aucss, erglm_data, family = poisson())
mod_pois

}
