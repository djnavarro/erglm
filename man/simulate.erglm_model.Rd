% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/erglm-simulate.R
\name{simulate.erglm_model}
\alias{simulate.erglm_model}
\title{Simulate responses from an exposure-response model}
\usage{
\method{simulate}{erglm_model}(object, nsim = 1, seed = NULL, ...)
}
\arguments{
\item{object}{An erglm model, as returned by \code{\link[=erglm_model]{erglm_model()}}}

\item{nsim}{Number of replicates}

\item{seed}{Used to set the RNG seed. If \code{NULL}, a random seed is
chosen and reported.}

\item{...}{Ignored}
}
\value{
A tibble with one row per observation per simulated
replicate, containing:
\itemize{
\item \code{dat_id}, \code{sim_id}: identifiers for the original observation and
the simulation replicate
\item \code{mu}: the expected response (response scale) at the sampled
parameter vector
\item \code{val}: the simulated response value (\code{mu} plus family-appropriate
noise)
\item one \verb{coef_*} column per model coefficient (e.g. \verb{coef_`(Intercept)`},
\code{coef_aucss}), giving the sampled parameter values used for that
replicate -- prefixed to avoid colliding with predictor columns of
the same name
\item the model's predictor columns (not including the response)
}
}
\description{
Generates simulated response datasets from a fitted erglm model,
propagating uncertainty in the parameter estimates. Useful for
simulation-based confidence bands, predictive checks, or bootstrapping
downstream analyses. Implements the standard \code{stats::simulate()}
generic, so it is called as \code{simulate(object, ...)} rather than through
an erglm-specific function name.
}
\details{
Samples new parameter values from the multivariate normal
distribution implied by the model's variance-covariance matrix (via
\code{mvtnorm::rmvnorm()}), evaluates the expected response at each sampled
parameter vector using \code{\link[=erglm_fun]{erglm_fun()}}, then draws a simulated
response at each prediction using family-appropriate residual noise
(the same \code{.erglm_draw_response()} mechanism used by
\code{\link[=erglm_vpc_sim]{erglm_vpc_sim()}}: Bernoulli draws for \code{binomial}, Poisson draws for
\code{poisson}, normal draws for \code{gaussian}, gamma draws for \code{Gamma}). The
dispersion parameter used for that noise is a single point estimate
(\code{summary(object)$dispersion}), not resampled per replicate. Other
\code{glm()} families are not currently supported and will raise an
informative error.

\code{\link[=erglm_vpc_sim]{erglm_vpc_sim()}} is a thin wrapper around this method: it calls
\code{simulate()} internally, then drops the sampled coefficients and \code{mu}
and splices the simulated response (\code{val}) back into the response
column's original name, to produce a VPC-ready data set. Use
\code{simulate()} directly when you want the full simulation detail
(sampled parameters, expected and simulated response, one row per
observation per replicate); use \code{erglm_vpc_sim()} when you just want
a VPC-shaped data set.
}
\examples{
mod <- erglm_model(ae1 ~ aucss + sex, erglm_data, family = binomial())
simulate(mod, nsim = 5, seed = 963)

}
